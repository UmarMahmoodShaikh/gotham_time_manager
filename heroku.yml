build:
  docker:
    web: Dockerfile

run:
  web:
    command: /app/bin/gotham_time_manager start
    image: web
    environment:
      PHX_SERVER: true

release:
  command: '/app/bin/gotham_time_manager eval "GothamTimeManager.Release.migrate"'
